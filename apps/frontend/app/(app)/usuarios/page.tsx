export default function UsuariosPage() {
  return (
    <main className="app-page">
      <section className="shell reveal">
        <header className="page-header">
          <div>
            <h1 className="hero-title">Usuarios</h1>
            <p className="hero-copy">
              Veja quem tem acesso e organize os perfis.
            </p>
          </div>
        </header>
        <section className="card">
          <p className="helper">
            Nenhum usuario cadastrado ainda. Aqui vai aparecer a lista completa.
          </p>
        </section>
      </section>
    </main>
  );
}
